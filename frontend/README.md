# Portfolio Tracker Frontend

Next.js dashboard for the local Portfolio Tracker backend.

## Development

```bash
npm run dev
```

The dashboard is available at `http://localhost:3000/dashboard`.

Backend requests run on the Next.js server and use `BACKEND_BASE_URL`.
